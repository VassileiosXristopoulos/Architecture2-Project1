for checking if number in range (minInt64,maxInt64)
	MAX_EXPONENT = 62
	MANTISSA_BITNUM = 52
	exponent = exp - bias
	if(exponent>0){
		if(exponent > MAX_EXPONENT){
			if(sign>0)
				number = maxInt64
			else
				number = minInt64
		}
		else{
			if(exponent > MANTISSA_BITNUM){
				number =(sign)*( mantissa << exponent - 52 )
			}
			else{
				number = (sign)*( mantissa >> 52 - exponent )
			}
		}
	}
	else{ // negative exponent, round 
	
	}